# 项目健康检查（结构 / 质量 / 安全 / 文档 / 测试 / CI）

> 最近一次检查：2026-04-07

## 概览
- 仓库以 Markdown 知识库为主，代码集中在 `Proj/` 的示例脚本。
- **已整改**：补全标准 Python `.gitignore`，并清理误提交的 `.pyc` / `__pycache__` 文件。
- **主要风险**：暂无自动化测试与 CI；缺少安全流程（漏洞披露、密钥使用约束）；部分目录（如 `rhetoric/`）缺少使用指引。

## 分类检查与建议
- **结构**：目录层级清晰；建议为主要子模块补充 `README`（如 `rhetoric/`）以描述用途与维护方式。
- **代码质量**：脚本体量小，未配置格式化 / lint；如需继续扩展，可考虑 `ruff/black` 或 `pre-commit`。
- **安全**：依赖 `.env` 提供密钥，需确保不写入代码库；缺少安全策略文档和漏洞反馈渠道，建议添加 `SECURITY.md`。
- **文档**：根 README 完整，子模块文档不均衡；建议补充示例运行截图或常见问题。
- **测试**：无自动化测试；可先为 `Proj` 添加最小化 smoke test（例如对话函数的假响应或接口封装层的输入校验）。
- **CI**：无 CI；待测试方案确定后，配置基础流水线（lint + smoke test）即可。

## 整改跟踪
| 状态 | 类别 | 事项 | 说明 |
| --- | --- | --- | --- |
| ✅ 完成 | 仓库卫生 | 添加标准 Python `.gitignore` | 覆盖 `.env`、缓存、构建产物，避免敏感信息与临时文件再次入库 |
| ✅ 完成 | 仓库卫生 | 清理已提交的 `.pyc` / `__pycache__` | 删除误提交的编译缓存 |
| ⏳ 待办 | 文档 | 为 `rhetoric/` 增补使用说明与自定义指引 | 帮助读者按场景快速定位和复用话术 |
| ⏳ 待办 | 安全 | 添加 `SECURITY.md`（漏洞反馈、密钥管理约束） | 规范问题上报与密钥使用要求 |
| ⏳ 待办 | 测试 | 设计 `Proj` 的最小化 smoke test | 可通过 mock / contract 检查避免真实外部调用 |
| ⏳ 待办 | CI | 配置基础 CI（lint + smoke test） | 等待测试方案落地后引入 |

> 如需复查：先完成待办项，再依据同一分类维度复核，并记录结论与日期。
